
<div class="container">
<h1 class="title">The Burger Bar</h1>


<div class="bodyText">
<h2 class="subText"><i>Pick a pre-made burger or create your own!</i></h2>

<br><br>

<h4>Add a Burger</h4>
<div class="row col-md-12">
<form class="create-form">

  <div class="form-group">
    <label for="bu">Name:</label>
    <input class="inputBox" type="text" id="bu" name="name">
    <button type="submit">Save</button>
  </div>
  
  <div class="form-group">
    <label for="devoured">Have you eaten this burger before?</label><br>
    <input type="radio" name="devoured" value="1" checked> Yes, I've eaten this one..<br>
    <input type="radio" name="devoured" value="0"> Not yet!
  </div>

  
</form>
</div>


<div class="row">
<div class="col-md-6">
<h4>Menu:</h4>
<ul>
   {{#each burgers}}
      {{#if devoured}}
        {{> burgers/burger-block devoured=false}}
      {{/if}}
    {{/each}}
  </ul>
</div>


<div class="col-md-6">
  <h4>You devoured these burgers!!</h4>
  <ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burgers/burger-block devoured=true}}
    {{/unless}}
  {{/each}}
</ul>
</div>
</div>
</div>
</div>


